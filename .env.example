# Copy this file to .env and fill in real values.
# .env is gitignored by default — never commit real secrets.

# --- App ---
NODE_ENV=development
PORT=3000
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# VITE_APP_URL=http://localhost:5173

# --- API keys / secrets (uncomment and rename what you actually need) ---
# DATABASE_URL=
# API_SECRET_KEY=
# NEXTAUTH_SECRET=
